const apiConstants = {
    API_URL: "http://localhost:4000/jsonplaceholder.typicode.com",
    USER: "/user",
    LOGIN: "/login",
    POSTS: "/posts",
    PHOTOS: "/photos",
    BEARER: "Bearer"
}

export default apiConstants;